import {describe,expect,it} from 'vitest';
import {generateFamilyCards,generateTalentOffer,isThemeSelectionValid,toggleTalentSelection,toggleThemeSelection} from '../src/domain/generators';
import {worlds} from '../src/data/worlds';
import {LocalWorldGenerationProvider} from '../src/world-generation/provider';
describe('AI 世界生成框架',()=>{it('为模板提供恰好10个差异化标签',async()=>{const p=new LocalWorldGenerationProvider();const modern=await p.generateThemeTags(worlds['modern-city']);const wuxia=await p.generateThemeTags(worlds['martial-arts']);expect(modern).toHaveLength(10);expect(new Set(modern).size).toBe(10);expect(modern).toContain('职场');expect(wuxia).toContain('门派');expect(wuxia).not.toEqual(modern)});it('结构化生成完整世界',async()=>{const p=new LocalWorldGenerationProvider();const w=await p.generateWorld({template:worlds.cultivation,selectedThemes:['宗门','长生','因果']});expect(w.name).toContain('修仙');expect(w.socialStructure).toBeTruthy();expect(w.loveMarriageRules).toBeTruthy();expect(w.birthRegions.length).toBeGreaterThan(0)})});
describe('严格选择限制',()=>{it('主题只允许3到5个有效，最多5个',()=>{let x:string[]=[];for(const t of ['a','b','c','d','e','f'])x=toggleThemeSelection(x,t);expect(x).toEqual(['a','b','c','d','e']);expect(isThemeSelectionValid(x)).toBe(true);expect(isThemeSelectionValid(['a','b'])).toBe(false)});it('天赋最多3个且可取消',()=>{let x=['a','b','c'];expect(toggleTalentSelection(x,'d')).toEqual(x);expect(toggleTalentSelection(x,'b')).toEqual(['a','c'])})});
describe('开局生成',()=>{it('家庭和天赋基于生成世界',()=>{const generated={templateId:'modern-city' as const,name:'新世界',selectedThemes:['科技','职场','家庭'],eraBackground:'',socialStructure:'',coreConflict:'',livingEnvironment:'',growthPaths:'',loveMarriageRules:'',familyStructure:'',dangers:'',lifespan:'',birthRegions:['AI新城'],overview:'',generatedAt:''};expect(generateFamilyCards('modern-city','',generated).every(x=>x.location==='AI新城')).toBe(true);expect(generateTalentOffer('modern-city','',generated)).toHaveLength(10)})});
